#include<bits/stdc++.h>

using namespace std;


void solve(int notneed){
    // int n;
    // cin >> n;

    // vector<int> a(n);
    // for(int i = 1; i <= n; i++) a[i-1] = i;

    // int presum = 0;
    // set<int> s;
    // for(int i = 0; i < n; i++) {
    //     presum += a[i];
    //     int bth = presum % n;
    //     if(s.count(bth+1)) break;
    //     s.insert(bth+1);
    // }

    // if(s.empty() || s.size() != n)  {
    //     cout << -1 << endl;
    //     return;
    // }
    // for(auto it : s) cout << it << " ";
    // cout << endl;
}


int main(){
  int T;
  cin >> T;

  for(int i = 0; i < T; i++)
    solve(i+1);
}
