#include<bits/stdc++.h>

using namespace std;


void solve(int t){
  string s;
  cin >> s;

  vector<int> v(s.length());
  for(int i = 0; i < s.length(); i++){
    v[i] = s[i]-'a'+1;
  }


  int mc = abs(abs[0] - abs[v.size()-1]);
  vector<int> res;
  for(int i = 0; i < v.size(); i++){
    
  }

  
}

void recurse(vector<int> t, int mc, int idx){
  for(int i = 0; i < t.size(); i++){
  }
}


int main(){
  int T;
  cin >> T;

  for(int i = 0; i < T; i++)
    solve(i+1);
}
